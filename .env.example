# Backend environment variables
DATABASE_URL=sqlite:///./appletv.db

# URL, по которому Apple TV загружает склеенный поток (720p/1080p со звуком).
# Укажите IP этого Mac в локальной сети и порт бэкенда (8000).
STREAM_BASE_URL=your_machine_ip:8000

# Frontend (обычно задаётся в docker-compose.yml)
# PUBLIC_API_URL=http://localhost:8000
